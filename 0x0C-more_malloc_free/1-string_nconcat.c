#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * string_nconcat - concatenates two strings
 * @s1: first string
 * @s2: second string
 *
 * Return: concatenation result
 */
char *string_nconcat(char *s1, char *s2, unsigned int n)
{
	char *res, i, j, len1, len2;

	len1 = strlen(s1);
	len2 = 0;

	if (strlen(s2) > 0)
		
}
