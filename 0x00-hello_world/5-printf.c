#include <stdio.h>

/**
 * main - Entry point
 * printf - use to print text
 * Description: use printf to display data
 * Return: always 0 means success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,%s");
	return (0);
}
