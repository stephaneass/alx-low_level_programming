#include "main.h"

/**
 * _strcpy - copies the string pointed to by
 * @dest: buffer pointed
 * @src: source string
 * Return: the pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	dest = src;

	return (dest);
}
