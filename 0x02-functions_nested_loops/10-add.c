#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: number 1 to compute
 * @b: number 2 to compute
 * Return: absolute value
 */
int add(int a, int b)
{
	return (a + b);
}
